/**
 * Skills List Module custom CSS fields definition.
 *
 * @package LeadersPath
 * @since 0.1.0
 */

import metadata from './module.json';

/**
 * CSS fields configuration for the Skills List module.
 *
 * This object maps CSS field names to their selector configurations,
 * used by both the VB styles component and PHP module styles.
 *
 * @since 0.1.0
 */
export const cssFields = metadata.customCssFields;
