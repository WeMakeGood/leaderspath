/**
 * Course Activities Module placeholder content.
 *
 * @package LeadersPath
 * @since 0.1.0
 */

import { CourseActivitiesAttrs } from './types';

/**
 * Default placeholder content for Course Activities module.
 *
 * @since 0.1.0
 */
export const placeholderContent: CourseActivitiesAttrs = {
  title: {
    innerContent: {
      desktop: {
        value: 'Course Activities',
      },
    },
  },
  emptyState: {
    innerContent: {
      desktop: {
        value: 'No activities have been assigned to this course.',
      },
    },
  },
};
